[package]
name = "ssm"
version = "0.1.0"
edition = "2024"
description = "A Ratatui session manager for Claude Code and Codex"
license = "MIT"

[dependencies]
chrono = "0.4"
crossterm = "0.29"
ratatui = "0.30.2"
serde_json = "1"
unicode-width = "0.2"

[profile.release]
lto = true
codegen-units = 1
strip = true


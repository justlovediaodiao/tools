# sync

sync file or text.

```
Usage of ./sync:
  -d string
        serve directory (default "./")
  -l string
        listen address or port (default "80")
  -s string
        service name, ftp or file or text
```
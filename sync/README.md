# sync

sync file or text.

```
Usage of ./sync:
ftp
  -d string
        serve directory (default "./")
  -l string
        listen address or port (default "80")
file
  -d string
        serve directory (default "./")
  -l string
        listen address or port (default "80")
text
  -l string
        listen address or port (default "80")
```
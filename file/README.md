# file

download/upload file or paste text.

```
Usage of ./file:
down
  -d string
        serve directory (default "./")
  -l string
        listen address or port (default "80")
up
  -d string
        serve directory (default "./")
  -l string
        listen address or port (default "80")
text
  -l string
        listen address or port (default "80")
```